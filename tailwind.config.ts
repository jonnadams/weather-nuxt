import type { Config } from 'tailwindcss'

export default <Partial<Config>>{
  theme: {
    extend: {
      colors: {
        brand: '#1e40af', // Example color; replace with your desired color
      },
    },
  },
}
